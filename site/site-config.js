// This file is regenerated automatically on every Vercel build (see build.sh),
// using your SUPABASE_URL and SUPABASE_ANON_KEY environment variables — the
// same ones already used to build the Flutter app.
//
// The empty values below are placeholders only, so the site doesn't error out
// if opened locally without running a real build. Do not paste real values
// here directly — they'll just get overwritten on the next deploy anyway.
window.__SUPABASE_URL = "";
window.__SUPABASE_ANON_KEY = "";
